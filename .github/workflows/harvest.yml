name: Harvest CC News

on:
  schedule:
    - cron: '30 2 * * *'
  workflow_dispatch:

jobs:
  harvest:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run harvester
        run: python harvester.py

      - name: Classify offers
        run: python classify_offers.py

      - name: Classify devaluations
        run: python classify_devaluations.py

      - name: Classify launches
        run: python classify_launches.py

      - name: Commit updated feed.json, deals.json, devaluations.json and launches.json
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'chore: update feed.json, deals.json, devaluations.json and launches.json [skip ci]'
          file_pattern: feed.json deals.json devaluations.json launches.json
